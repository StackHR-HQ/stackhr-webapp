// Spend-scoped approval queue — distinct from the global Approvals page in
// features/dashboard/.
export function SpendApprovalsPage() {
  return <h1 className="text-xl font-medium text-ink">Spend Approvals</h1>
}
