VITE_API_BASE_URL=/api

# Auth endpoints don't exist on the backend yet, so the app runs against a
# mock implementation (src/features/auth/mock-api.ts) by default. Set this
# to false once the real /auth/login and /auth/forgot-password routes exist.
VITE_USE_MOCK_AUTH=true
